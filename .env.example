# Optional Umami analytics configuration.
# Leave both values empty to disable analytics without build warnings.
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# Mobile (APK) only: absolute URL of the deployed backend, e.g. https://agenten-villa.onrender.com
# Leave empty for web builds (relative /api/trpc).
VITE_API_URL=
